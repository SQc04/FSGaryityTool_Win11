<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="FSGaryityTool_Win11.Controls.ScrollwaveformView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:FSGaryityTool_Win11.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:win2Dcanvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
             mc:Ignorable="d">

    <Grid>
        <Border x:Name="ScrollwaveformViewBackgroundBorder" Background="{x:Bind Background, Mode=OneWay}" BorderBrush="{x:Bind BorderBrush, Mode=OneWay}" BorderThickness="{x:Bind BorderThickness, Mode=OneWay}" CornerRadius="{x:Bind CornerRadius, Mode=OneWay}">
            <Grid>
                <win2Dcanvas:CanvasControl x:Name="LineDemonstratorCanvasControl" Margin="{x:Bind ControlBorderMargin, Mode=OneWay}" Draw="LineDemonstratorCanvasControl_Draw" />
                <win2Dcanvas:CanvasControl x:Name="WaveformDemonstratorCanvasControl" Margin="{x:Bind ControlBorderMargin, Mode=OneWay}" Draw="WaveformDemonstratorCanvasControl_Draw" />
            </Grid>
        </Border>
    </Grid>
</UserControl>

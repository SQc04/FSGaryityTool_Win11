<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="FSGaryityTool_Win11.Controls.PolyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:FSGaryityTool_Win11.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:win2D="using:Microsoft.Graphics.Canvas.UI.Xaml"
             mc:Ignorable="d">

    <Grid>
        <Border x:Name="PolyViewBackgroundBorder" Background="{x:Bind Background, Mode=OneWay}" BorderBrush="{x:Bind BorderBrush, Mode=OneWay}" BorderThickness="{x:Bind BorderThickness, Mode=OneWay}" CornerRadius="{x:Bind CornerRadius, Mode=OneWay}">
            <win2D:CanvasControl x:Name="LineDemonstratorCanvasControl" Margin="{x:Bind ControlBorderMargin, Mode=OneWay}" Draw="LineDemonstratorCanvasControl_Draw" Opacity="{x:Bind WaveGridBorderOpacity, Mode=OneWay}" />
        </Border>
    </Grid>
</UserControl>

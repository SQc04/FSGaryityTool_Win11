<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="FSGaryityTool_Win11.Controls.PolyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:FSGaryityTool_Win11.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:win2D="using:Microsoft.Graphics.Canvas.UI.Xaml"
             mc:Ignorable="d">

    <UserControl.Resources>
        <!--  Right-click menu item styles with icons  -->
        <Style x:Key="PolyViewMenuFlyoutItemAddStyle" TargetType="MenuFlyoutItem">
            <Setter Property="Icon">
                <Setter.Value>
                    <SymbolIcon Symbol="Add" />
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PolyViewMenuFlyoutItemDeleteStyle" TargetType="MenuFlyoutItem">
            <Setter Property="Icon">
                <Setter.Value>
                    <SymbolIcon Symbol="Delete" />
                </Setter.Value>
            </Setter>
        </Style>
        <!--  Context menus as XAML templates  -->
        <MenuFlyout x:Key="PolyViewCanvasMenu">
            <MenuFlyoutItem Click="CanvasMenu_AddPoint_Click" Style="{StaticResource PolyViewMenuFlyoutItemAddStyle}" Text="Add Point Here" />
        </MenuFlyout>

        <MenuFlyout x:Key="PolyViewThumbMenu">
            <MenuFlyoutItem Click="ThumbMenu_InsertBefore_Click" Style="{StaticResource PolyViewMenuFlyoutItemAddStyle}" Text="Insert Before" />
            <MenuFlyoutItem Click="ThumbMenu_InsertAfter_Click" Style="{StaticResource PolyViewMenuFlyoutItemAddStyle}" Text="Insert After" />
            <MenuFlyoutItem Click="ThumbMenu_Delete_Click" Style="{StaticResource PolyViewMenuFlyoutItemDeleteStyle}" Text="Delete Point" />
        </MenuFlyout>
    </UserControl.Resources>

    <Grid>
        <Grid x:Name="PolyViewBackgroundBorder" Background="{x:Bind Background, Mode=OneWay}" BorderBrush="{x:Bind BorderBrush, Mode=OneWay}" BorderThickness="{x:Bind BorderThickness, Mode=OneWay}" CornerRadius="{x:Bind CornerRadius, Mode=OneWay}">
            <win2D:CanvasControl x:Name="LineDemonstratorCanvasControl" Margin="{x:Bind ControlBorderMargin, Mode=OneWay}" Draw="LineDemonstratorCanvasControl_Draw" Opacity="{x:Bind WaveGridBorderOpacity, Mode=OneWay}" />
            <Canvas x:Name="PolyEditDemonstratorCanvas" Margin="{x:Bind ControlBorderMargin, Mode=OneWay}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Canvas.Resources>
                    <!--  Thumb template for our PolyView thumbs (simple circular appearance)  -->
                    <Style x:Key="PolyViewThumbTemplate" TargetType="Thumb">
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Background" Value="{ThemeResource SliderThumbBackground}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Thumb">
                                    <Border Margin="-4" Background="{ThemeResource SliderOuterThumbBackground}" BorderBrush="{ThemeResource SliderThumbBorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{ThemeResource SliderThumbCornerRadius}">
                                        <Ellipse x:Name="SliderInnerThumb" Width="{ThemeResource SliderInnerThumbWidth}" Height="{ThemeResource SliderInnerThumbHeight}" Fill="{TemplateBinding Background}" RenderTransformOrigin="0.5, 0.5">
                                            <Ellipse.RenderTransform>
                                                <CompositeTransform />
                                            </Ellipse.RenderTransform>
                                        </Ellipse>

                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualState x:Name="Normal">
                                                    <Storyboard>
                                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)">
                                                            <!--  0.86 is relative scale from 14px to 12px  -->
                                                            <SplineDoubleKeyFrame KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" KeyTime="{StaticResource ControlFastAnimationDuration}" Value="0.86" />
                                                        </DoubleAnimationUsingKeyFrames>
                                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)">
                                                            <!--  0.86 is relative scale from 14px to 12px  -->
                                                            <SplineDoubleKeyFrame KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" KeyTime="{StaticResource ControlFastAnimationDuration}" Value="0.86" />
                                                        </DoubleAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </VisualState>

                                                <VisualState x:Name="PointerOver">
                                                    <Storyboard>
                                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)">
                                                            <!--  1.167 is relative scale from 12px to 14px  -->
                                                            <SplineDoubleKeyFrame KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" KeyTime="{StaticResource ControlNormalAnimationDuration}" Value="1.167" />
                                                        </DoubleAnimationUsingKeyFrames>
                                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)">
                                                            <!--  1.167 is relative scale from 12px to 14px  -->
                                                            <SplineDoubleKeyFrame KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" KeyTime="{StaticResource ControlNormalAnimationDuration}" Value="1.167" />
                                                        </DoubleAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </VisualState>

                                                <VisualState x:Name="Pressed">
                                                    <Storyboard>
                                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)">
                                                            <!--  0.71 is relative scale from 14px to 10px  -->
                                                            <SplineDoubleKeyFrame KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" KeyTime="{StaticResource ControlNormalAnimationDuration}" Value="0.71" />
                                                        </DoubleAnimationUsingKeyFrames>
                                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)">
                                                            <!--  0.71 is relative scale from 14px to 10px  -->
                                                            <SplineDoubleKeyFrame KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" KeyTime="{StaticResource ControlNormalAnimationDuration}" Value="0.71" />
                                                        </DoubleAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </VisualState>

                                                <VisualState x:Name="Disabled">
                                                    <Storyboard>
                                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)">
                                                            <!--  1.167 is relative scale from 12px to 14px  -->
                                                            <SplineDoubleKeyFrame KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" KeyTime="{StaticResource ControlFastAnimationDuration}" Value="1.167" />
                                                        </DoubleAnimationUsingKeyFrames>
                                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)">
                                                            <!--  1.167 is relative scale from 12px to 14px  -->
                                                            <SplineDoubleKeyFrame KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" KeyTime="{StaticResource ControlFastAnimationDuration}" Value="1.167" />
                                                        </DoubleAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </VisualState>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Canvas.Resources>
            </Canvas>
        </Grid>
    </Grid>
</UserControl>
